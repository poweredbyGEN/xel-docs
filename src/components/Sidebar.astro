---
/**
 * Override of Starlight's left Sidebar — same default page tree, with a Search
 * box pinned to the TOP of the sidebar.
 */
import Search from 'virtual:starlight/components/Search';
import MobileMenuFooter from 'virtual:starlight/components/MobileMenuFooter';
import SidebarPersister from '@astrojs/starlight/components/SidebarPersister.astro';
import SidebarSublist from '@astrojs/starlight/components/SidebarSublist.astro';

const { sidebar } = Astro.locals.starlightRoute;
---

<a class="xel-sidebar-logo" href="/" aria-label="XEL Docs home">
	<img src="/logo.svg" alt="XEL" />
</a>

<div class="gen-sidebar-search">
	<Search />
</div>

<SidebarPersister>
	<SidebarSublist sublist={sidebar} />
</SidebarPersister>

<div class="md:sl-hidden">
	<MobileMenuFooter />
</div>
