---
title: "Reference"
description: "Canonical schemas, modules, invariants, events, and API surfaces."
---

# Reference

Look things up. Rendered from the build spec, which is the source of truth, so it never drifts.

- Capability schemas: inference.v1, ingestion.v1, retrieval.v1, treasury.v1, distillation.v1, voice.v1, image.v1, video.v1, full request and response.
- Move modules: every on-chain module, its types, and its public functions.
- The aNFT object: every field, its meaning, and its mutability.
- Invariants: the full enforced list, each with the guarantee it provides.
- Error codes: the consolidated table.
- Events: every emitted event and when it fires.
- Payment modes: up-to, exact, and hold, and when each applies.
- The usage record: every metered dimension by modality.
- Glossary: every term, one line each.
- Client API: the reference client methods, once the SDK surface is finalized.

---
