# PR-only documentation build mirror. GitHub Pages deployment remains in
# .github/workflows/deploy.yml and is intentionally not reproduced here.
when:
  event: pull_request

steps:
  - name: build
    image: node:20-bookworm
    commands:
      - npm ci
      - npm run build
      - test -f dist/index.html
